function getNum(arr){
  if(arr.length <=1){
    return arr
  }
  const n = arr.length

  const res = new 
  // for(let i = 0,l=arr.length;i++){
  //   res.push(dfs([i, ...dfs([...arr.splice(i, 1)])])
  // }
}

console.log(getNum([1,2,3])); // 6,